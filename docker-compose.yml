services:
  postgres:
    image: postgres:15-alpine
    container_name: nexus-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-nexus_collab}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: nexus-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  qdrant:
    image: qdrant/qdrant:latest
    container_name: nexus-qdrant
    restart: unless-stopped
    ports:
      - "${QDRANT_PORT:-6333}:6333"
    volumes:
      - qdrant_data:/qdrant/storage

  nexus-ai:
    build:
      context: ./nexus-ai
    container_name: nexus-ai
    restart: unless-stopped
    ports:
      - "${NEXUS_AI_PORT:-8000}:8000"
    environment:
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}
      NEXUS_AI_MODEL: ${NEXUS_AI_MODEL:-openai/gpt-4o-mini}
      NEXUS_AI_API_KEY: ${NEXUS_AI_API_KEY:-change-me}
      NEXUS_AI_DEBUG_LOG: ${NEXUS_AI_DEBUG_LOG:-false}
      NEXUS_AI_RAW_PROVIDER_CAPTURE: ${NEXUS_AI_RAW_PROVIDER_CAPTURE:-false}
      NEXUS_AI_RAW_RESPONSE_DIR: ${NEXUS_AI_RAW_RESPONSE_DIR:-raw-provider-responses}
      NEXUS_BACKEND_BASE_URL: ${NEXUS_BACKEND_BASE_URL:-http://host.docker.internal:3002/api/v1}
      NEXUS_INTERNAL_API_TOKEN: ${NEXUS_INTERNAL_API_TOKEN:-change-me}
      OPENROUTER_APP_URL: ${OPENROUTER_APP_URL:-http://localhost:5175}
      OPENROUTER_APP_TITLE: ${OPENROUTER_APP_TITLE:-Nexus}
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 5s
      retries: 3


  seaweed-master:
    image: chrislusf/seaweedfs:latest
    container_name: seaweed-master
    command: "master -ip=seaweed-master -port=9333 -defaultReplication=000"
    ports:
      - "9333:9333"
    volumes:
      - seaweed_master:/data
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:9333/cluster/status"]
      interval: 10s
      timeout: 5s
      retries: 3

  seaweed-volume:
    image: chrislusf/seaweedfs:latest
    container_name: seaweed-volume
    command: "volume -mserver=seaweed-master:9333 -port=8080 -dir=/data -max=100 -preStopSeconds=1"
    volumes:
      - seaweed_volume:/data
    depends_on:
      - seaweed-master

  seaweed-filer:
    image: chrislusf/seaweedfs:latest
    container_name: seaweed-filer
    command: "filer -master=seaweed-master:9333 -port=8888"
    ports:
      - "8888:8888"  # Filer UI + API
    volumes:
      - seaweed_filer:/data
    depends_on:
      - seaweed-master
      - seaweed-volume
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8888/"]
      interval: 10s
      timeout: 5s
      retries: 3

  seaweed-s3:
    image: chrislusf/seaweedfs:latest
    container_name: seaweed-s3
    command: "s3 -filer=seaweed-filer:8888 -port=8333"
    ports:
      - "8333:8333"  # S3 API endpoint
    environment:
      # Có thể override trong .env
      WEED_S3_ACCESS_KEY: ${SEAWEED_ACCESS_KEY:-accesskey}
      WEED_S3_SECRET_KEY: ${SEAWEED_SECRET_KEY:-secretkey}
    depends_on:
      - seaweed-filer

  # nexus-mcp:
  #   build:
  #     context: ./nexus-mcp
  #   container_name: nexus-mcp
  #   restart: unless-stopped
  #   ports:
  #     - "${NEXUS_MCP_PORT:-8000}:8000"
  #   environment:
  #     MCP_SERVER_NAME: ${MCP_SERVER_NAME:-autopilot_core}
  #     NEXUS_INTERNAL_API_URL: ${NEXUS_INTERNAL_API_URL:-http://host.docker.internal:3002/api/v1/internal/mcp}
  #     NEXUS_INTERNAL_API_TOKEN: ${NEXUS_INTERNAL_API_TOKEN:-change-me}
  #     REQUEST_TIMEOUT_SECONDS: ${REQUEST_TIMEOUT_SECONDS:-30}
  #   extra_hosts:
  #     - "host.docker.internal:host-gateway"

  # meilisearch:
  #   image: getmeili/meilisearch:latest
  #   container_name: nexus-meilisearch
  #   restart: unless-stopped
  #   ports:
  #     - "${MEILISEARCH_PORT:-7700}:7700"
  #   environment:
  #     MEILI_MASTER_KEY: ${MEILISEARCH_MASTER_KEY:-test_master_key}
  #     MEILI_ENV: development
  #   volumes:
  #     - meilisearch_data:/meili_data

  # typesense:
  #   image: typesense/typesense:27.1
  #   container_name: nexus-typesense
  #   restart: unless-stopped
  #   ports:
  #     - "${TYPESENSE_PORT:-8108}:8108"
  #   environment:
  #     TYPESENSE_API_KEY: ${TYPESENSE_API_KEY:-test_api_key}
  #     TYPESENSE_DATA_DIR: /data
  #   volumes:
  #     - typesense_data:/data
  #   command: --data-dir /data --api-key=${TYPESENSE_API_KEY:-test_api_key}

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  qdrant_data:
    driver: local
  seaweed_master:
    driver: local
  seaweed_volume:
    driver: local
  seaweed_filer:
    driver: local
  # meilisearch_data:
  #   driver: local
  # typesense_data:
  #   driver: local
